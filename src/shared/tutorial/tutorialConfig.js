/**
 * Master switch for the site-user guided tour (offer dialog + "?" FAB).
 * Set to `true` to show and run the tutorial on mobile/tablet site-user routes.
 * Set to `false` to hide the "?" button and disable tour offer / auto-start.
 */
export const TUTORIAL_ENABLED = false
