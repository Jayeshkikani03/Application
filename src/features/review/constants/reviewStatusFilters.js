/** Fixed review-list filters (aligned with review workflow statuses from API rows). */
export const DEFAULT_REVIEW_STATUS_FILTER = "";

export const VISIBLE_REVIEW_STATUSES = new Set(["In Progress", "Submitted", "Reviewed"]);
